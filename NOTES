todo: write a build and patch script for protocol.c in original enet source code below:

int exported_enet_protocol_dispatch_incoming_commands (ENetHost * host, ENetEvent * event){
  return enet_protocol_dispatch_incoming_commands (host, event);
}
int exported_enet_protocol_handle_incoming_commands (ENetHost * host, ENetEvent * event){
  return enet_protocol_handle_incoming_commands (host, event);
}
int exported_enet_protocol_send_outgoing_commands (ENetHost * host, ENetEvent * event, int checkForTime$
  return enet_protocol_send_outgoing_commands (host, event, checkForTimeouts);
}

